import 'package:flutter/material.dart';

class DistressCallScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Distress Call')),
      body: Center(child: Text('Distress Call Screen')),
    );
  }
}